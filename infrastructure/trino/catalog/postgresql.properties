# =============================================================================
# Trino PostgreSQL Catalog Configuration
# =============================================================================
# Direct SQL queries on PostgreSQL metadata tables
# =============================================================================

connector.name=postgresql
connection-url=jdbc:postgresql://postgres:5432/agentdb
connection-user=admin
connection-password=admin123
